{
  "name": "postapp",
  "version": "1.0.0",
  "description": "Native iOS wrapper for POSTAPP",
  "scripts": {
    "cap:ios": "npx cap add ios && npx cap sync ios",
    "cap:sync": "npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}